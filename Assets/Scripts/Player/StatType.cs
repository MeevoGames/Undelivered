namespace Undelivered.Player
{
    /// <summary>Player stat kinds. Used for HUD spaces and floating value texts.</summary>
    public enum StatType
    {
        Gold,
        Trust,
        Dice
    }
}
