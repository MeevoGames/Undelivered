using UnityEngine;

namespace Undelivered.Work
{
    /// <summary>
    /// Marker for the draggable quality-stamp element. The player drags it onto a box in the
    /// enlarged view to stamp it. Put it on a UIDraggable with "Return To Start On Drop" enabled so
    /// it snaps back after each use.
    /// </summary>
    public class QualityStamp : MonoBehaviour
    {
    }
}
