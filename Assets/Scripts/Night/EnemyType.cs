namespace Undelivered.Night
{
    /// <summary>
    /// An enemy's type, used to trigger combat synergies (certain types together, or all the same).
    /// Rename / extend these as the roster grows.
    /// </summary>
    public enum EnemyType
    {
        Neutral,
        Bestia,
        Maquina,
        Espectro,
        Elemental
    }
}
